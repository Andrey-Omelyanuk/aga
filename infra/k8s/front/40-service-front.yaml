apiVersion: v1
kind: Service
metadata:
  name: aga-front
  namespace: aga
spec:
  type: NodePort
  selector:
    app: aga-front
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30082