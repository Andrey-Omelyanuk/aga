# 🎬 aga — Ага, и готово.

> *«— Сделаешь?  
> — Ага.»*  
> *(Двое из ларца, одинаковых с лица, 1974)*

Легковесный Rust-фреймворк для LLM-агентов. **Специально разработан для слабых (маломощных) LLM** — архитектура максимально упрощена, чтобы даже модели уровня 7B и меньше могли эффективно работать без сложных orchestration-систем. Один контейнер, прямой доступ к Docker, встроенная трассировка, human-in-the-loop. Без оркестраторов, очередей и облачного оверхеда.

## 💡 Философия проекта

- **🎯 Создан для слабых LLM** — главная цель системы: работа с маломощными моделями (7B и меньше). Каждая деталь архитектуры оптимизирована под «глупые» модели: минимальное количество шагов, простые промпты, отсутствие сложных цепочек рассуждений. Сложные фичи приносятся в жертву простоте.
- **🧹 Код проектов должен оставаться чистым** — агенты и их конфиги (`config/roles.yaml`) вынесены за пределы основного кода. Разработчики могут использовать любые инструменты и подходы, не засоряя общую кодовую базу настройками агентов. Никакого мусора в коде проекта.
- **🤝 Агенты общаются друг с другом** — все агенты могут взаимодействовать между собой через API. Например, frontend-агент может запросить API у backend-агента для получения объекта, и backend-агент вернёт ему данные. Frontend-агенту не нужно сканировать документацию бэкенда — он просто делает запрос к другому агенту.
- **🐳 Самоорганизация на основе Docker Compose** — вся система работает внутри Docker-контейнеров. Вы просто указываете путь к `docker-compose.yml`, а готовые роли автоматически подхватывают свою часть конфигурации. Агенты самоорганизуются на основе того, что уже есть в проекте: они видят сервисы, понимают их назначение и распределяют задачи без дополнительной настройки.

---

## ✨ Ключевые возможности

| Фича | Описание |
|------|----------|
| 🐳 **Один контейнер** | Нет NATS, Redis, S3. Все роли работают в одном процессе |
| 🐳 **Docker-нативный** | Прямое выполнение команд через `docker`. Управление сервисами, перезапуск, деплой |
| 📊 **Встроенная трассировка** | SQLite (WAL-режим) хранит каждый шаг, команду, ответ LLM и human-запросы. Доступно через `/trace/{id}` |
| 🤝 **Human-in-the-loop** | Агент может задать вопрос через `[ASK_HUMAN]...[/ASK_HUMAN]`. Ожидание ответа через API |
| ⚡ **Гибкий LLM-бэкенд** | Любой OpenAI-compatible провайдер (Ollama, vLLM, OpenAI, LocalAI). Настраивается на каждую роль отдельно |
| 🎯 **Локальный запуск** | Оптимизирован для локальной разработки. Минимум кода, минимум промптов |

---

## 🏗️ Архитектура

```
[HTTP Client / CLI / CI]
│
▼ POST /tasks/{role}
┌─────────────────────────────────────────────────┐
│ aga (Rust container)                            │
│ ┌───────────────────────────────────────────┐   │
│ │ • roles.yaml (конфиг ролей)               │   │
│ │ • trace.db (SQLite, WAL)                  │   │
│ │ • Agent Loop (LLM → parse → exec → trace) │   │
│ │ • Axum HTTP API                           │   │
│ └───────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
│ (docker)
▼
[Docker Daemon]
│
├── [service-1] [service-2] [service-N]
└── [новые сервисы по запросу]
```

---

## 🚀 Быстрый старт

### 1. Подготовка
```bash
mkdir -p data/work
```

### 2. Конфигурация ролей
Отредактируйте `config/roles.yaml` (пример ниже).

### 3. Запуск
```bash
docker compose up --build -d
docker compose logs -f
```

### 4. Тест
```bash
# Отправить задачу
curl -X POST http://localhost:8080/tasks/docker-helper \
  -H "Content-Type: application/json" \
  -d '{"task": "Покажи список запущенных контейнеров"}'

# Посмотреть трассировку (замените UUID из ответа)
curl http://localhost:8080/trace/<task-uuid> | jq
```

## ⚙️ Конфигурация (config/roles.yaml)

```yaml
roles:
  docker-helper:
    prompt: |
      Ты — ассистент по Docker. Управляй контейнерами и сервисами.
      Разрешённые: docker, echo.
      Перед перезапуском сервисов спрашивай подтверждение.
    allowed_commands: ["docker", "echo"]
    max_iterations: 8
    llm:
      model: "qwen2.5-coder:7b"
      temperature: 0.2

  app-deployer:
    prompt: "Ты — деплоер приложений. Поднимай новые сервисы через docker."
    allowed_commands: ["docker", "echo"]
    max_iterations: 12
    llm:
      model: "gpt-4o-mini"
      temperature: 0.3
```

## 🌐 API Reference

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/tasks/{role}` | Запуск задачи. Тело: `{"task": "..."}`. Возвращает `{"status": "ok", "task_id": "uuid", "result": "..."}` |
| GET | `/trace/{task_id}` | Полная трассировка шагов (JSON-массив) |
| GET | `/human/pending` | SSE-стрим ожидающих human-запросов |
| POST | `/human/answer/{id}` | Ответ на вопрос. Тело: `{"answer": "..."}` |

## 🛡️ Безопасность

- 🔐 Доступ к Docker socket требует доверия к агенту (локальный запуск)
- 📁 Валидация `allowed_commands` перед выполнением
- ⏱️ Лимит итераций на задачу
- 📊 Полная трассировка всех действий в SQLite

⚠️ **Важно**: Эта версия предназначена только для локального запуска в доверенной среде. Для production добавьте изоляцию и аудит.

## 📁 Структура проекта

```
aga/
├── docker-compose.yml
├── Cargo.toml
├── Dockerfile
├── config/
│   └── roles.yaml
├── data/                    # создаётся при первом запуске
│   ├── trace.db
│   └── work/
└── src/
    ├── main.rs
    ├── config.rs
    ├── trace.rs
    ├── llm.rs
    ├── agent.rs
    └── server.rs
```

## 🔜 TODO / Roadmap

### 🧠 Ядро и агент
- Контекстное сжатие: автоматическая `summarize()` истории каждые N шагов
- Hot-reload `roles.yaml` без перезапуска контейнера
- Улучшенный парсер LLM-ответов (игнорирование markdown-обёрток, поддержка JSON-структур)

### 📡 Наблюдаемость и API
- SSE/WebSocket стрим: live-вывод шагов, статусы, human-запросы в реальном времени
- `/health` и `/metrics` (Prometheus-совместимые метрики)
- Экспорт трассировки в JSON/Markdown для отчётов

### 🚀 DX
- CLI-утилита `aga-cli` для удобной работы
- Примеры промптов для типовых задач (деплой, мониторинг, логирование)

## 📜 Лицензия

MIT. Делайте что хотите. Ага. 🎬
